apiVersion: v1
kind: Service
metadata:
  name: ms-auth
  labels:
    app: ms-auth
spec:
  selector:
    app: ms-auth
  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.port }}
